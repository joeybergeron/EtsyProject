<div class="grid">
	<% _.forEach(results, function(listings) { %>
		<div class="<%= listings.listing_id %> listings">
			<img src="<%= listings.MainImage.url_170x135 %>">
			<h3><%= listings.title %></h3>
		</div>
	<% }); %>
</div>
